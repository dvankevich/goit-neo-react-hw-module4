## 📝 Тест-План (Test Plan) для Сайту Галереї

### 1. Вступ (Introduction)

* **Приклад роботи** ([https://goitlmsstorage.b-cdn.net/ad72f8ff-de6a-4ce6-ad50-b1ce531d43d003-image-finder%20%281%29.mp4](Відео))

* **Назва Проєкту:** Сайт Галереї ([https://goit-neo-react-hw-module4-brown-phi.vercel.app/](https://goit-neo-react-hw-module4-brown-phi.vercel.app/))
* **Мета Тестування:** Перевірити, що функціональні можливості сайту галереї, зокрема відображення зображень, пошук та відображення великого зображення (модальне вікно), працюють відповідно до очікувань та без дефектів.
* **Обсяг Тестування (Scope):**
    * Функціональне тестування (Пошук, відображення, відкриття модального вікна, кнопки/лінкі).
    * Тестування інтерфейсу користувача (UI) та взаємодії з користувачем (UX).
    * Тестування сумісності (навігатори: Chrome, Firefox - основні).
    * Тестування продуктивності/навантаження (обмежується перевіркою швидкості завантаження та відображення *загальної* кількості зображень).

### 2. Стратегія Тестування (Testing Strategy)

* **Типи Тестування:**
    * **Smoke Testing:** Перевірка основних функцій (завантаження сторінки, пошук, відкриття зображення).
    * **Functional Testing:** Детальна перевірка кожного елементу інтерфейсу та його поведінки.
    * **UI/UX Testing:** Перевірка зовнішнього вигляду, відповідності макету та зручності використання.
    * **Exploratory Testing:** Неформальне тестування для виявлення неочевидних дефектів.
* **Середовище Тестування:**
    * **Тестовий стенд:** Production/Staging URL ([https://goit-react-hw-04-zeta-umber.vercel.app/](https://goit-react-hw-04-zeta-umber.vercel.app/)).
    * **Браузери:** Google Chrome (остання версія), Mozilla Firefox (остання версія).
    * **Пристрої:** Desktop/Laptop.

### 3. Критерії Входу та Виходу (Entry and Exit Criteria)

| Критерій Входу (Entry Criteria) | Критерій Виходу (Exit Criteria) |
| :--- | :--- |
| Додаток розгорнуто на тестовому стенді та доступний. | Усі критичні (Severity 1) та високі (Severity 2) дефекти виправлені та перевірені. |
| Тест-план та тест-кейси затверджені. | Понад 95% критичних та високих тест-кейсів пройшли успішно. |
| Тестові дані (зображення) доступні. | Звіт про тестування складено та затверджено. |

### 4. Ресурси та Розклад (Resources and Schedule)

* **Ресурси:** Тестувальник (Ви), Браузери, Інструменти розробника (DevTools).
* **Тривалість:** Залежить від деталізації, але для основних функцій орієнтовно 1-2 години.

---

## 📋 Тест-Кейси (Test Cases)

Я розіб'ю їх на основні функціональні групи.

### Група 1: Пошук Зображень (Search Functionality)

| ID | Опис Тест-Кейсу (Title) | Кроки Тестування (Steps) | Очікуваний Результат (Expected Result) |
| :--- | :--- | :--- | :--- |
| **TC-SRCH-001** | Пошук за валідним ключовим словом. | 1. Відкрити головну сторінку. 2. Увести валідне слово (наприклад, "cat") у поле пошуку. 3. Натиснути кнопку "Search". | Відображається галерея зображень, що відповідають пошуковому запиту "cat". Поле пошуку та кнопка "Search" залишаються на сторінці. |
| **TC-SRCH-002** | Пошук за неіснуючим ключовим словом. | 1. Відкрити головну сторінку. 2. Увести неіснуюче/унікальне слово (наприклад, "asdfghjklqwe") у поле пошуку. 3. Натиснути кнопку "Search". | Відображається повідомлення про відсутність результатів (наприклад, "No results found" або подібне). Галерея порожня. |
| **TC-SRCH-003** | Пошук за порожнім запитом. | 1. Відкрити головну сторінку. 2. Залишити поле пошуку порожнім. 3. Натиснути кнопку "Search". | Пошук не виконується. Може бути попереджувальне повідомлення (наприклад, "Please enter a search query"). |
| **TC-SRCH-004** | Пошук з великої/малої літери (Case Insensitivity). | 1. Здійснити пошук за словом "Dog". 2. Здійснити пошук за словом "dog". | Результати пошуку в обох випадках ідентичні. |

### Група 2: Відображення Галереї (Gallery Display)

| ID | Опис Тест-Кейсу (Title) | Кроки Тестування (Steps) | Очікуваний Результат (Expected Result) |
| :--- | :--- | :--- | :--- |
| **TC-GALL-001** | Відображення початкового завантаження. | 1. Відкрити головну сторінку. | Відображаються зображення за замовчуванням (якщо є) або відображається повідомлення/стан очікування. |
| **TC-GALL-002** | Функціональність кнопки "Load More" (Завантажити ще). | 1. Виконати пошук, що дає багато результатів. 2. Перевірити, чи відображається кнопка "Load More". 3. Натиснути кнопку "Load More". | До існуючих зображень додається нова партія зображень. Кнопка "Load More" залишається, доки є нові зображення для завантаження. |
| **TC-GALL-003** | Поведінка кнопки "Load More" після вичерпання результатів. | 1. Здійснити пошук, що дає обмежену кількість результатів (менше, ніж 2 партії). 2. Натиснути "Load More" доти, доки зображення перестануть завантажуватись. | Кнопка "Load More" зникає або стає неактивною. Може бути повідомлення про кінець результатів. |
| **TC-GALL-004** | Відображення мініатюр. | 1. Виконати будь-який пошук. 2. Оцінити відображені мініатюри. | Усі зображення мають коректно відображатися (не бути "битими" або порожніми). Розмітка мініатюр має бути коректною. |

### Група 3: Модальне Вікно (Modal Window/Large Image View)

| ID | Опис Тест-Кейсу (Title) | Кроки Тестування (Steps) | Очікуваний Результат (Expected Result) |
| :--- | :--- | :--- | :--- |
| **TC-MOD-001** | Відкриття модального вікна натисканням на мініатюру. | 1. Виконати пошук. 2. Натиснути на одну з мініатюр зображень. | Відкривається модальне вікно. У ньому відображається те ж саме, але збільшене зображення. Задній фон має бути затемненим. |
| **TC-MOD-002** | Закриття модального вікна клавішею ESC. | 1. Відкрити модальне вікно (див. TC-MOD-001). 2. Натиснути клавішу **ESC**. | Модальне вікно закривається. Сторінка галереї повертається до початкового стану (зображення залишаються на місці). |
| **TC-MOD-003** | Закриття модального вікна кліком на backdrop. | 1. Відкрити модальне вікно. 2. Натиснути на затемнену область навколо зображення (backdrop). | Модальне вікно закривається. |
| **TC-MOD-004** | Відсутність скролінгу під модальним вікном. | 1. Відкрити модальне вікно. 2. Спробувати прокрутити сторінку. | Основна сторінка під модальним вікном не прокручується. |

### Група 4: UI/UX (User Interface and User Experience)

| ID | Опис Тест-Кейсу (Title) | Кроки Тестування (Steps) | Очікуваний Результат (Expected Result) |
| :--- | :--- | :--- | :--- |
| **TC-UI-001** | Перевірка відсутності горизонтального скролу. | 1. Відкрити сторінку в стандартному браузерному вікні (Desktop). | На сторінці відсутній горизонтальний скрол. |
| **TC-UI-002** | Респонсивність (зміна розміру вікна). | 1. Відкрити сторінку. 2. Зменшувати ширину вікна браузера. | Елементи галереї (мініатюри) перегруповуються відповідно до ширини, не порушуючи загальний макет. |
| **TC-UI-003** | Видимість стану завантаження (Loading State). | 1. Виконати пошук. 2. Відстежити поведінку інтерфейсу під час очікування відповіді від сервера. | На час завантаження з'являється індикатор (Spinner, "Loading..." текст) або інший візуальний елемент, що інформує користувача про очікування. |

---

Звісно! Валідація пошукового поля (Search Input Validation) є критичною для запобігання помилок та забезпечення коректної роботи.

Ось детальні тест-кейси, сфокусовані саме на валідації вводу у пошуковому полі.

---

## 📋 Тест-Кейси: Валідація Пошукового Поля (Search Input Validation)

| ID | Опис Тест-Кейсу (Title) | Кроки Тестування (Steps) | Очікуваний Результат (Expected Result) | Пріоритет |
| :--- | :--- | :--- | :--- | :--- |
| **TC-VAL-001** | **Порожній запит** (Empty Query) | 1. Залишити поле пошуку порожнім. 2. Натиснути кнопку "Search" або `Enter`. | Пошук **не повинен виконуватися**. Користувач повинен побачити повідомлення (наприклад, спливаюче вікно, тост або текст) про те, що поле не може бути порожнім: "Please enter a search query." | High |
| **TC-VAL-002** | **Пробіли як запит** (Whitespace Only) | 1. Увести кілька пробілів у поле пошуку (наприклад, `"   "`). 2. Натиснути кнопку "Search" або `Enter`. | Поведінка, як при порожньому запиті (див. TC-VAL-001). Пошук не виконується, відображається повідомлення про порожній запит. **Або:** Якщо система тримує пробіли, то пошук повинен виконатися, але повернути **відсутність результатів** (якщо пробіли не є валідним пошуковим терміном). | High |
| **TC-VAL-003** | **Спеціальні символи** (Special Characters) | 1. Увести рядок, що складається лише зі спеціальних символів (наприклад, `"!@#$%^"`). 2. Натиснути "Search". | Пошук повинен обробити ці символи, але, швидше за все, повернути **відсутність результатів**, оскільки вони рідко відповідають зображенням. **Важливо:** Не повинно бути помилок на рівні програми чи API. | Medium |
| **TC-VAL-004** | **Символи та текст** (Mixed Input) | 1. Увести запит, що містить валідне слово та спеціальні символи (наприклад, `"nature&flowers"`). 2. Натиснути "Search". | Система повинна або **ігнорувати** спеціальні символи та шукати `"nature flowers"`, або виконувати пошук за точним запитом. Результати повинні бути релевантними. | Medium |
| **TC-VAL-005** | **Максимальна довжина запиту** (Max Length) | 1. Спробувати ввести дуже довгий рядок (наприклад, 256 символів). | Поле вводу повинно **обмежити** кількість символів, щоб запобігти надмірному навантаженню на API. Якщо обмеження немає, пошук повинен виконатися, і система має коректно обробити запит без збою. | Low |
| **TC-VAL-006** | **Ввід чисел** (Numeric Input) | 1. Увести рядок, що складається лише з чисел (наприклад, `"12345"`). 2. Натиснути "Search". | Пошук повинен коректно виконатися, якщо числа є валідними пошуковими термінами (наприклад, рік або номер моделі). | Medium |
| **TC-VAL-007** | **Регістр** (Case Sensitivity) | 1. Пошук: `"Cat"`. 2. Пошук: `"cat"`. 3. Пошук: `"cAt"`. | Результати для всіх трьох запитів повинні бути **ідентичними** (більшість галерейних API є регістронезалежними). | High |
| **TC-VAL-008** | **HTML/JS ін'єкція** (Security/XSS) | 1. Увести потенційно небезпечний рядок (наприклад, `<script>alert('XSS')</script>`). 2. Натиснути "Search". | Система повинна **санітизувати** (очистити) ввід. Ніяких скриптів не повинно бути виконано, і ніяких HTML-тегів не повинно бути видно на сторінці. | Critical |

### Додаткові перевірки (Focus on UI/UX)

1.  **Плейсхолдер:** Перевірити, чи відображається текст-підказка (Placeholder) у полі пошуку (наприклад, "Search images and photos...").
2.  **Фокус:** Перевірити, чи встановлюється фокус на поле пошуку при завантаженні сторінки або після виконання пошуку.
3.  **Очищення поля:** Після успішного пошуку перевірити, чи залишається введений запит у полі (зазвичай залишається).
4.  **Копіювання/Вставляння (Copy/Paste):** Перевірити, чи можна успішно скопіювати та вставити довгий рядок у поле пошуку.

